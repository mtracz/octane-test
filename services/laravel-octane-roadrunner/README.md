# hosts file
Add\
`laravel-octane-roadrunner.local`\
to the hosts file

#make commands
```
make shell
make run
make build
make stop
```

# run roadrunner server
```
php artisan octane:start --host 0.0.0.0
```